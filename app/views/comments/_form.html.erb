<% form_for([@post, @post.comments.build]) do |f| %>
  <%= f.hidden_field :post_id %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
