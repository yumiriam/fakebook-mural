<% new_post = Post.new %>
<%= form_for(new_post, remote: true) do |f| %>
  <% if new_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% new_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b>New post:</b><br />
    <%= f.text_field :content, :class => "text_field_mural" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
